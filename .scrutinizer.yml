build:
  environment:
    php: 7.2
  dependencies:
    override:
      - composer install --no-interaction --prefer-dist
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
  paths: ["app/*"]

tools:
  external_code_coverage:
    timeout: 600


